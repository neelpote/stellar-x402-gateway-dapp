[package]
name = "access-controller"
version.workspace = true
edition.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk.workspace = true

[dev-dependencies]
data-registry = { path = "../data-registry" }
soroban-sdk = { workspace = true, features = ["testutils"] }
